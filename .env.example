# Set these in Vercel → Project → Settings → Environment Variables.
# DATABASE_URL is added automatically when you connect a Neon Postgres database.
DATABASE_URL=
# The password for your private /admin page. Make it long.
ADMIN_PASSWORD=
